import json
import boto3


def lambda_handler(event, context):
    client_dynamo-boto3.resource('dynamodb') #same as coloumn names and keep all string
    table-client_dynamo.Table('Student')
    response-table.put_item(Item-event)
    return response
    
   
